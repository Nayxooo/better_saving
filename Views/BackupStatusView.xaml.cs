using System.Windows.Controls;

namespace better_saving.Views
{
    public partial class BackupStatusView : System.Windows.Controls.UserControl
    {
        public BackupStatusView()
        {
            InitializeComponent();
        }

        // later → methods to refresh / bind the data
    }
}
