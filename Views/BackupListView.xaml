<UserControl x:Class="better_saving.Views.BackupListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:better_saving"
             xmlns:converter="clr-namespace:better_saving.Converters"
             Background="#FF1E1E1E">
    <UserControl.Resources>
        <converter:NullOrEmptyToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converter:JobStateToIconConverter x:Key="JobStateToIconConverter"/>
        <converter:IconColorConverter x:Key="IconColorConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                  Text="{DynamicResource BackupJobListTitle}"
                  Foreground="White"
                  FontSize="25"
                  Margin="10"/>

        <!-- Liste des backups -->
        <ListView Grid.Row="1"
                  x:Name="JobsListView"
                  ItemsSource="{Binding Jobs}"
                  SelectedItem="{Binding SelectedBackupJob}"
                  Background="Transparent"
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="Border"
                                        Background="#FF161618"
                                        CornerRadius="10"
                                        BorderThickness="0">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF22272A"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF22272A"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Icône d'état -->
                        <Path Grid.Column="0"
                              Data="{Binding State, Converter={StaticResource JobStateToIconConverter}}"
                              Fill="{Binding State, Converter={StaticResource IconColorConverter}}"
                              Width="20"
                              Height="20"
                              Stretch="Uniform"
                              Margin="0,0,10,0"/>

                        <!-- Nom du backup -->
                        <TextBlock Grid.Column="1"
                                  Text="{Binding Name}"
                                  Foreground="White"
                                  VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>